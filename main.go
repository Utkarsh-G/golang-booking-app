// Initialize project in terminal with `go mod init golang-booking-app`

package main

import (
	"fmt"
)

// declare an entrypoint, ie main. 1 main per application
func main() {
	fmt.Print("Hello World")
	fmt.Println("hello again. whole line to myself")
}

// run it with `go run main.go`
